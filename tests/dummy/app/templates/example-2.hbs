<p>
  An action is fired when the bottom of the infinite scroll component becomes visible as the
  document is scrolled.
  <br>
  This will work even if there is other content on the page that is longer
  than the infinite scroll content.
</p>

{{#infinite-scroller
  class="example-2"
  onLoadMore=(action "loadMore")
  useDocument=true
  leeway="20%"
as |scroller|}}
  {{#each things as |thing|}}
    <div class="thing">{{thing.name}}</div>
  {{/each}}
  {{#if scroller.isLoading}}
    <div class="status">Loading...</div>
  {{/if}}
{{/infinite-scroller}}